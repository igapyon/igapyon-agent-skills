# __SKILL_TITLE__ References

Use this index to discover detailed references for `__SKILL_NAME__`.

Recommended reference groups:

- `workflow/` for workflow rules and examples
- `runtime/` for operation maps and runtime contracts
- `examples/` for focused request / result examples
